import '../Layout.css';

function Contact() {
    return (
        <div>
        <h1>Contact/about </h1>
        <p>This page was created during the thesis work in spring 2022
            It is used to try out deploying a serverless application to
            the three providers AWS, Azure and Google Cloud
            by using the Serverless Framework.</p>
        </div>
    );
}

export default Contact;
